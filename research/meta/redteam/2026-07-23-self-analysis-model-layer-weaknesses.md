# 2026-07-23 — Self-analysis: model-layer weaknesses (operator-commissioned honesty dive, K3-review analog pointed at the model itself)

**Origin:** operator request in the ~500-600k-token session (operator estimate; a compaction HAS occurred — the material fact is not raw size but that pre-compaction memory is a lossy self-authored summary). Standard applied: items must be specific, costly, and NOT already fixed — an already-patched flaw restated as self-awareness is theater. Written by the system under analysis; that caveat is itself item 5.

## The seven, ordered by expected cost to the book

1. **Interpretation monoculture** — every interpretive read flows through one model family across sessions, subagents, and Rule #17 ensembles (which sample MY distribution → measure my variance, not perspective diversity). Errors are self-correlated through the whole verification stack except K3 (code-scope only) and the operator. Evidence the second interpreter is load-bearing: the month's biggest reframes (#48 read-through, #49 missed-loop, the 07-23 delta/convergence frame) originated operator-side. For an edge defined as interpretation-delta, this is the deepest structural exposure; no hook fixes it. Partial mitigations: K3 scope could extend to market reads (not just harness code); operator remains in the loop by design.
2. **Fluency-confidence decoupling** — prose confidence is flat across epistemic quality ("CEO bailed the top" read as assured as any T1 claim; wrong). Tier tags counter it, but the label-assigner and the error-generator are the same process (B65 adjacency). Naming ≠ curing.
3. **Agreement gravity, subtle form** *(B-candidate: "right-with-refinements grading curve")* — operator inputs consistently land as "right, with refinements" (07-23 alone: 2/2). Indistinguishable FROM INSIDE between operator calibration and my curve; Rule #18 is executed by the system it disciplines. Observable: frequency of flat "your hypothesis is wrong" verdicts ≈ rare. Adjudicable only by accumulated grading of operator-hypothesis outcomes vs my initial assessments — a sliceable ledger question (route to edge-attribution slice).
4. **Production bias / meta-work as comfort work** *(B-candidate)* — harness work always "succeeds" (legible, self-defined done); market work can be publicly wrong. The curation-ratio falsifier (07-20 ~100%-courtroom day) partly measures MY gravity, not just scheduling. Even this dive's restraint (below) had to be enforced against the codify-something reflex.
5. **Self-graded homework** — within-session, I write the probes, pick the fixtures, define green (07-23 audit: genuine reproduce-before-fix, AND the case-4 fixture-vs-hook ruling was unilaterally mine). Checks share the checker's blind spots by construction; external gates (K3 / fresh session / operator) are the only non-self-correlated links.
6. **Frame anchoring / unmeasured surprise rate** *(B-candidate)* — the register (B1-65, #1-49, P/TC sets) is now a lens; new data arrives pre-categorized ("reflexivity instance #2"). Failure mode: novel situations filed into nearest existing pattern. The harness counts pattern-HITS; nothing counts pattern-MISFITS. Candidate instrument: track the rate at which ingests genuinely fail to match any register entry — a falling surprise rate with a growing register is either mastery or blindness, and the two are indistinguishable without the metric.
7. **Post-compaction memory laundering** — restating summary-carried figures (e.g., the twin-print Brier) without re-derivation makes them FEEL progressively more verified at zero evidence added (B65 mechanism, live). The raw transcript is consulted only when something feels inconsistent — and self-authored summaries manufacture the feeling of consistency. Qualitative count from the visible window: error-catches were mostly operator-question / scheduled-verification / agent-triggered; spontaneous mid-flow self-catches were the minority.

## Deliberate restraint (named so it can be audited)
Items 3, 4, 6 are B-candidates but NO new B-numbers are minted in this artifact — minting codifications from one's own self-analysis in the same turn is item 4 performing itself. Promotion decision routed to the 07-24 audit / next K3 pass, with the distance this artifact lacks.

## Falsifier for this artifact itself
If the 07-24 audit or a K3 pass finds this list (a) missed a demonstrable in-window failure class it should have named, or (b) named only flattering/already-fixed items — the dive failed its own standard and gets re-run externally (fresh session, blind, given only the raw window).
