# 2026-07-23 — Fable-5 harness-audit instance: P0 hardening executed (07-24 docket, run early per NO-DELAY)

**Origin:** operator enabled a dedicated Fable-5 budget for harness work ("the harness update is then for today"). This instance ran the 07-24 P0 docket's executable core under reproduce-before-fix discipline: every claimed hole probed pre-fix (failing probe = reproduction receipt), patched, re-probed, suite-pinned. Commits: `4b26e72` (checkpoint 1), `e477487` (checkpoint 2). Model provenance note (B63/B64): this audit was executed by a different model tier than the one that built the guards — same independence logic as the K3 cross-family reviews, one family removed.

## Executed (receipts in the two commits + test suites)

| # | Item | Pre-fix probe | Fix | Post-fix |
|---|---|---|---|---|
| a | git-guard `commit -n` regression (07-21 tempered token refused to cross `-m`/`-F`) | a2/a4/a5/a6 all exit 0 (4 bypass shapes) | data-span stripping (echo/printf args, non-shell heredocs) + whole-segment scan, `n` anywhere in bundle; `bash -c` payloads stay scanned (pinned) | 25/25 scratchpad probe; adjacency suite 12/12 incl. 4 new pins; old documented-residual echo-prose FP RESOLVED (fixture flipped 2→0) |
| b | git-guard rm boundary (`~/LLMNA/`, `~/`, `$HOME/LLMNA`, `${HOME}` forms) | b1–b4 all exit 0 | rm tail regex + REPO_TOKENS extended (repo-scoped tilde/HOME aliases; bare `~` handled in rm branch only — deliberately NOT in REPO_TOKENS to avoid arming repo gates on every home-dir mention) | all 8 rm-bypass shapes BLOCK; 3 legit-rm controls still ALLOW |
| c | structural-output PI matcher case-brittle (line 171) | lowercase `position implication:` escaped the tier gate | `re.IGNORECASE` | 6 casing fixtures green (lower/UPPER/Title, tiered + untiered + above-line variants) |
| d | `test_structural_output_tier_gate` case-4 born-red + hardcoded path | reproduced RED; root cause = the HOOK, not the fixture: house-standard forms ("joint-state matrix" hyphenated — the exact CLAUDE.md/priming spelling — and "H1 (P=60%)" inline weights) were never credited by the marker list | hook marker list widened; fixture kept as-was (its expectation was right); all 3 test files made CLAUDE_PROJECT_DIR-relative | 10/10 |
| 1 | **G-28 numerator reclassification** (BLOCKING pre-08-06) | contaminated numerator counted tier-gate fires, probe fires, and a line self-labeled "do not count" | numerator = `structural-markers-missing` only; tier fires → report-only column; probe/smoke/KNOWN_PROBE excluded with in-code rationale; legacy untyped (6 lines, 06-13..18) counted for continuity | **MATERIAL: flips the 08-06 read.** Contaminated: 0.131→0.217 FLAT/RISING (= retire priming). Clean: 0.120→0.105 FALLING (= keep). The contamination was about to adjudicate the experiment wrongly. Final adjudication still lands 08-06 on the remaining window. |
| 2 | **e04eaef ref-order** (BLOCKING pre-08-06) | local-main-first vs design-130 | ADJUDICATED: `origin/main` first (canonical pushed history; stale local main shrinks the denominator = the 0.196↔0.452 swing), then `main`, then `HEAD` — 07-21 never-crash chain kept | documented in-code |
| 5 | **redirect over-block FP** (16 blocks 07-19..22 incl. the twin-print commit block 07-22 23:46) | reproduced: heredoc-written commit-message file whose BODY contains `>` + a protected filename | redirect check scans the data-stripped view (`s_redir`): heredoc bodies (unless shell-fed) and quoted spans (unless `sh -c` payloads) are text | FP shapes r1–r3 ALLOW; true-positives r4–r6 (unquoted redirect onto protected, `sh -c` payload redirect, heredoc TARGETING a protected file) still BLOCK; 4 fixtures pinned in repo suite |
| — | **probe-pollution channel** (G-28's 2nd channel) — demonstrated live: checkpoint-1 probes wrote ~100 untagged BLOCK/FIRE lines into the fire-log | — | `LLMNA_PROBE=1` env in all 3 test harnesses; both instrumented hooks append ` probe=1`; metric excludes; KNOWN_PROBE_FIRES retro-list for today's pre-tagging fires | verified end-to-end (tagged lines visible in fire-log) |

**Documented residuals (in-code, confused-agent threat model):** quoted-flag `git commit "-n"`; heredoc piped into a non-`*sh` interpreter; `git commit -uno` over-blocks (rephrase long-form); `bash -c` heredoc-to-shell scanning preserved.

## NOT executed this instance (dispositions — remain on the 07-24 item)
1. **G-19 cascade-hook changeset window** (diff vs last-Stop ref, not bare HEAD) — real hole (K3 refuted the masking rationale); needs a stored last-Stop ref design; do at 07-24 audit proper.
2. **stop-hook-git-check 8-exit-path inventory** — real gap is the inventory, not the doc bullet; mechanical but deserves its own pass.
3. **K3 Q5 blind spots** — (i) blanket `stop_hook_active` guard = enforcement-free Stop after every block (design tension with the infinite-loop guard; needs per-hook decision + review before touching LIVE enforcement — Rule #19 HIGH-adjacent); (ii) stdin-schema-drift invisible-death class; (iii) 337-agent journal receipt gap.
4. **exit-1-outside-repo FP** + **~150 MEDIUM/LOW extract items** — untriaged backlog, 07-24.
5. **Receipts hook Phase 1** (P1) — build spec exists; ships only through the pre-registered review gate (corpus FP backtest → K3 + fresh-session review → wire). Not a solo-ship item; unchanged.
6. **NEW finding this instance:** `test_framework_codifications.py` 198/200 RED **pre-existing at HEAD** (verified via stash-bisect) — two stale todo.md content-couplings ("Claim-verification audit cycle" prose the test expects was pruned). Test needs re-scoping to durable anchors; added to the 07-24 list.
7. **Fire-log rotation/cap** (P2 fire-logging item) — today's probe runs made the growth-rate case vividly; unchanged on its docket slot.

**Falsifier / re-eval (this audit's own):** if any of the 6 shipped fixes produces a user-visible FP or a probe-verified bypass within 14 days (re-eval 2026-08-06 with the metric adjudication), the fix reverts to its pre-audit form and the failure gets a 3-layer GRADE. The redirect-FP fix specifically: if a genuine truncating redirect onto a protected file ALLOWS in that window, that is a CATASTROPHIC-class miss — revert immediately and re-probe.
