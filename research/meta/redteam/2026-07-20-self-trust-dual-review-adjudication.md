# 2026-07-20 — Self-trust build: DUAL red-team adjudication (K3 blind + fresh-Claude in-repo) → merged verdict + SHIP-NOW package BUILT same turn
**Companion docs: input spec `2026-07-20-self-trust-build-redteam-input.md` · K3 adjudication `2026-07-20-self-trust-K3-adjudication.md` (5/5 checkable claims verified). This file adjudicates the SECOND review (fresh Claude session, in-repo, empirical) against the first, resolves their disagreements, and records what shipped.**

## What the in-repo review added that the blind review could not
1. **Mechanism (B) RESOLVED: "(B) is (A) in disguise" — and the premise itself was the bias in action.** H1 (grep timeout) killed empirically: the grounding grep over all 555 research/*.md files runs ~7ms mean against a 10,000ms timeout (fresh-Claude timing run) — a transient fire needs a ~1,400× slowdown. H3 (tree-state) killed by history: `30.2%` present since 2026-06-01 (AGC facts.md) / 2026-07-03 (ASMPT facts.md) — re-verified by orchestrator via `git log -S`, even earlier than the review stated. Remaining mechanism: the percentage regex's `\s*` captures **invisible unicode spaces** (NBSP U+00A0, U+202F, U+2009 — orchestrator re-verified by direct regex test), producing needles that fail `grep -F` and render near-identically to the no-space form in a block message. **The only evidence the original fire's token was "no-space" was how a block message was read and remembered — that transcript died with its container. The build spec's own STILL-OPEN premise was an unverifiable recall of un-logged state — B65 demonstrated recursively inside the B65 postmortem.**
2. **1c backtest with real numbers:** the spec's sketch = **0 false positives across 108,883 corpus sentences** — FP was never the problem; **recall** is: the first-person anchor misses the dominant third-person phrasing ("the hook fired three times today"). Sharpened trigger (count-word + harness-noun + past-window token, no first-person): still 0 corpus FPs, catches both phrasings, passes "I committed <hash>" / "I fired two agents in parallel".
3. **1a needs plumbing, not a copy-paste:** the diagnostics 1a-bis requires die inside `is_grounded_in_repo()` (bare bool, swallowing exceptions) — the logger must receive per-needle `(needle-repr, rc, exception)`; needles **repr()-escaped** or a U+202F needle reproduces its own illegibility inside the log and (B) stays "open" forever.
4. **grep semantics correction:** rc=1 is grep working correctly (clean no-match) — retry only on rc≥2/exception. (Amends K3's retry-on-nonzero.)
5. **Item 3 duplication:** the proposed latent-capability thread already exists as Principle #43's monthly affordance review + #43b behavioral-audit protocol (orchestrator re-verified: methodology.md #43 with standing review question + falsifier, wired to the 07-24 audit item). A second thread violates the don't-duplicate constraint.
6. **Live triple-confirmation:** the anti-fab hook fired on the review message itself via mechanism (A) — quoted with-space needles — 5× on one token, every instance a *mention* (quoted needle), not a *claim*.
7. **Loophole audit with computed magnitudes:** round-number context-blindness (orchestrator recount on main: a bare `50%` grounds against 308 files) — exact-string grounding structurally cannot catch fabricated common round numbers; only unusual-precision numbers are strongly protected.

## Cross-review disagreements → rulings
| Point | K3 (blind) | Fresh-Claude (in-repo) | Ruling |
|---|---|---|---|
| **Item 3** | ADOPT-AMEND (adoption gate) | REJECT (duplicates #43) | **REJECT the new thread** — in-repo evidence beats blind design. K3's adoption-gate criterion SALVAGED into the existing #43 affordance review (shipped: methodology.md #43 falsifier section). |
| **Item 2** | ADOPT-AMEND: rename "system-trust", avoidance-audit falsifier | ADOPT-AMEND: demote to "auditability layer" scoped principle, NOT objective-v3; canary falsifier | **Fresh-Claude's demotion adopted** (matches the orchestrator's own pre-registered doubt that Item 2 was the weakest link): shipped as #43b clause 3f — an auditability layer, not an objective tier. BOTH falsifier fixes merged: canaries (deterministic) + avoidance audit (sampling). K3's cross/within-session decomposition kept as framing (prosthetics-by-construction cross-session; trainable within-session). Fresh-Claude's correction adopted: "permanent" is untestable — formulation is *prosthetics-until-a-falsifier-says-otherwise*; the spec's ~45/~35/~30 weights were unfounded precision on N=1, dropped. |
| **1a-bis retry semantics** | three-valued, retry on nonzero | rc=1 = clean no-match, no retry; retry only rc≥2/exception | **Merged:** K3's three-valued architecture (GROUNDED/FABRICATED/INCONCLUSIVE; fire only on proven absence) + fresh-Claude's rc semantics. Shipped. |
| **1c** | REJECT sketch → recompute-and-compare | ADOPT-AMEND → sharpened tripwire trigger (no first-person), tool-use-in-turn exemption | **Merged, staged T+2wk (todo, due 2026-08-03):** fresh-Claude's trigger (backtested 0-FP) + K3's recompute-from-source-of-record action where a record exists; honestly labeled tripwire (low-FP, low-recall — "1a+1b are the real fix"), with fresh-Claude's retire falsifiers. |
| **Backtick/mention exemption** (fresh-Claude bonus suggestion) | — | exempt code-quoted tokens (mentions not claims) | **DEFERRED (Rule #18 dissent):** backtick-wrapping would be a trivial evasion channel for real claims; the whitespace-collapse already resolves the quoted-needle FP class that actually occurred (a quoted `'30.2 %'` now grounds via collapse). Revisit only if mention-FPs recur post-fix. |

## SHIPPED this commit (the merged ship-now package)
1. **`anti-fabrication-hook.py` rebuilt:** three-valued grounding (fire ONLY on FABRICATED = grep rc=1 proven absence; INCONCLUSIVE = rc≥2/exception after one retry → logged, exit 0 — the old `except: return False` fail-closed-on-infra path is dead); whitespace-collapse needle normalization (kills mechanism (A) incl. all unicode-space variants); per-needle diagnostics plumbed (`needle-repr | rc | exc`); `log_fire()` house-format with **verdict:pending** field (flip to TRUE-FIRE / FALSE-POSITIVE at adjudication; monthly audit samples flips); embedded `--selftest` — **PASS 8/8** (unicode-space grounding, proven-absence fires, infra-error INCONCLUSIVE, repr-escape, fail-open log). Fail-open wrapper on main.
2. **`llm-native-priming-hook.py`:** computed **TODAY date-header** at inject time (mechanism-3 fix — absolute dates in static prose proved non-salient) + **item 11** (harness-history counts are COMPUTED, never recalled — the priming bracket fresh-Claude flagged as missing). Live-tested: header + item 11 present in injection output.
3. **Codifications:** methodology **#43b clause 3f** (meta-state claims COMPUTED-class + auditability-layer scoped principle + canary/avoidance falsifier + round-number known-limit) · **B65 context-fluency** (biases-watchlist, CANDIDATE N=1, re-eval 2026-08-20) · #43 affordance-review **adoption gate** · CLAUDE.md/tags.md header tails synced.
4. **Staged (todo):** 1c tripwire (due 2026-08-03, after 2wk of log data) · log_fire house-standard rollout to remaining hooks (07-24 audit — fresh-Claude: fire evidence is ephemeral for most live hooks, the generalized gap) · session-prime per-item age tags (07-24) · European decimal-comma grounding normalization (parked, over-grounding risk).

## Accepted residuals (stated, not hidden)
- A real fabrication during genuine infra stress passes as INCONCLUSIVE (logged, visible at audit).
- Round-number fabrications structurally pass exact-string grounding (canaries therefore use unusual precision).
- The verdict-flip is an LLM discipline, not deterministic — monthly audit samples it; ≥2 wrong verdicts in a sample → redesign.
- A hard container kill between fire and commit still loses the log line — log-then-commit discipline (git-check enforces the commit) is what makes fires durable.
- The commissioning insight held: the two reviewers' catches were near-disjoint (K3: architecture, falsifier design, temporal decay; fresh-Claude: mechanism resolution, backtest numbers, duplication, recursion) — same non-overlap phenomenon as the instrument waves, now observed on harness self-governance.

**Position implication: NO ACTION (harness-meta; no market exposure). 🟢 for the shipped mechanism (selftest-verified, dual-red-teamed, live-fire triple-confirmed); 🟡 for the staged 1c (backtested but unbuilt). Net-positive test (Rule #11 Class-1): kills a demonstrated error class (un-computed self-claims + two confirmed FP mechanisms) for one hook-edit + one inject-edit; every piece ships with its own falsifier.**

---
## CORRECTIONS (2026-07-20, from the fresh-Claude ROUND-2 drift audit — no silent edits; the lines above stand as the record of what was claimed)
1. **"~45/~35/~30 weights ... dropped" is GARBLED (B65 instance, mine):** the spec's Item-2 triple was **~45/~20/~35** (H2/H1/H3); **~35/~35/~30** was the UNRELATED 1a-bis mechanism-hypothesis set. I blended two H-sets into one triple inside the artifact codifying the blending bias. The substantive ruling (weights were unfounded precision on N=1, dropped) stands.
2. **"5× on one token" is UNRECEIPTED:** the committed log has zero anti-fabrication lines from that session (old hook predated logging); the count exists only in a dead transcript. Status: reviewer-reported, unverifiable — not a repo fact.
3. **"0 FPs across 108,883 corpus sentences" is UNRECEIPTED:** no script, corpus dump, or output was committed; the number's only repo occurrences are its own restatements. The 1c pre-ship backtest must reproduce it, not cite it.
Full round-2 adjudication: `2026-07-20-self-trust-freshclaude-r2-adjudication.md`.
