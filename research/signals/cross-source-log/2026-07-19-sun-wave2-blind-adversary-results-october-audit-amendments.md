# 2026-07-19 SUN EVE — WAVE-2 BLIND-ADVERSARY RESULTS (6 instruments, 36 findings): commissioning complete for all material instruments; the OCTOBER AUDIT SPEC ITSELF AMENDED

**WORKFLOW: harness-meta (instrument commissioning v2, wave 2; per NO-DELAY rule, same-day). Protocol integrity: author worlds pre-committed at a2ac320; adversaries received I/O specs only. Full payloads in agent logs; this artifact = diffs + forced amendments of record.**

## Computed result
Author worlds 17 across 6 instruments; adversary findings 36. **Non-overlap ≈24/36 (~67%; wave-1 was 12/19 ≈63%)** — the protocol's yield is stable and high. Channel: referee-protocol. Commissioning falsifier (zero non-overlap = theater): decisively untouched across both waves.

## THE HEADLINE — five defects found INDEPENDENTLY by ≥2 blind adversaries (an accidental first concentration view, before the monthly audit ever computed one)
| Shared defect | Found by | Consequence |
|---|---|---|
| **A. Correlated forecasts void nominal n** (one worldview/one capex cycle drives many calls; effective n ≈ 1/3 of nominal; detection floors fictitious) | quota-5, brier-5, alpha-2, BR1-5 — **×4 independent** | Every power statement (0.071 floor, 150-call target, October decidability) must be cluster-robust or it is ceremony |
| **B. Self-coded outcomes + grading order** (same hand forecasts, times resolution, codes 0/1; wins grade faster) | brier-2, parser-6 — ×2 | Calibration ledger carries directional flattery risk; blinded re-code sample + latency-by-outcome monitor required |
| **C. Resolution-timing selection** (short-dated calls resolve first → October measures a different population than the program trades) | alpha-3, quota-6, brier-2b — ×3 | October verdicts describe the resolved subsample, not the forecaster |
| **D. Equal-weighted score ≠ portfolio value** (Brier on filler calls can misgrade the position-gating calls both directions) | brier-6, alpha-5 — ×2 | Stakes/decision-linkage tag per call; capture-weighted alpha alongside equal-weighted (coheres w/ objective fn v2) |
| **E. Benchmark not frozen/in-sample** (shrinkage fit on the outcomes it grades; class assignment = un-timestamped judgment by the audited pipeline) | brier-3, alpha-fragility — ×2 | Walk-forward benchmarks only; base-P assignments freeze at registration (P-provenance already timestamps — verify tally uses frozen values) |

## Per-instrument sharpest NON-OVERLAP catches (full lists in agent logs)
- **I-4 BR-1:** (i) WALK-DOWN world — 83% measures "clears a sandbagged bar," not "good quarter" (observable: T-90 vs T-1 consensus drift; beat-then-selloff fraction); (ii) **SNAPSHOT-VINTAGE mismatch — the 82.8% is a property of WHICH DAY'S estimate is frozen** (fix: derivation vintage MUST equal resolution vintage — one docs check); (iii) anchor-contamination — the base-P governs "beats consensus" but anchors questions it doesn't govern (reaction, guide) + evidence double-counting (AI-demand strength is already IN the 83%).
- **I-5 brier_tally:** (i) POOLED-BASE MIRAGE — benchmark must be per-class, not pooled ledger base (question-selection launders as skill); (ii) in-sample shrinkage = answer-key grading (the 0.1777 "beating us" is plausibly this artifact — verify script); (iii) missing RECALIBRATE branch (timidity = one-parameter fix, not a kill case).
- **I-6 parser:** (i) unparseable-date rows = obligations invisible FOREVER (worse than late); (ii) self-referential coverage (N and M from same regex — pipe-in-cell blinds both); (iii) "live" defined NEGATIVELY = both historical incidents re-derived blind; fix-shape: positive closed-vocabulary status field (converges w/ structured-data kill criterion).
- **I-7 quota:** decomposition inflation (one bet → 5 registrable children); easy-call stuffing mechanically FLATTERS the calibration curve; horizon-shortening censoring (quota+audit-date jointly select short-dated calls, no bad faith needed). Hardening adopted: count only calls with (a) min divergence from base, (b) pre-audit resolution, (c) declared correlation-cluster; power on clusters.
- **I-8 alpha-gate:** (i) Simpson/concentration — 8-for-8 above-base hits mathematically contribute POSITIVE alpha, so the aggregate negative is generated by an unexamined slice (leave-one-out required before any verdict); (ii) null-simulation guard — compute P(alpha≤observed | finals are true probabilities) before reading negative alpha as fact; (iii) **CONVERGENCE-TO-CONSENSUS — a promote-or-retire gate TEACHES price-copying (shrink to market odds = survival strategy = zero trading value)**: alpha must also be computed against market-implied odds; (iv) the k-fit menu: forecast = base + k·deviation, fit k — if interior k* beats both endpoints, both October options are dominated.
- **I-9 cap:** (i) bias-correction eviction has NO regression test (prophylactic knowledge looks like dead weight precisely because it works) → EVICTION LOG required (what was cut, when — enables the recurrence check); (ii) compression-drift — repeated LLM re-tightening erodes scope/magnitudes ("reduce by half when X and Y"→"be cautious") → entries link canonical source, re-derive-not-recompress after 2 rewrites; (iii) REGIME-ANCHORING — always-injecting regime priors converts one stale judgment into correlated errors across ALL sessions (edit-lag = the correlated-error window, measurable in git); (iv) the real variable is decision-relevance density, not bytes; tiered inject/retrieve architecture = candidate redesign, adjudicate at monthly audit (grounded: Chroma context-rot + lost-in-the-middle literature, adversary-cited).

## AMENDMENTS OF RECORD (booked same-commit into the program doc + base-rates + commissioning doc)
OCT-1 cluster-robust power everywhere (defect A); OCT-2 blinded re-code sample + grading-latency-by-outcome monitor at audit (B); OCT-3 resolved-vs-open distribution check before verdicts (C); OCT-4 stakes-tag per call + capture-weighted alpha (D); OCT-5 walk-forward benchmarks + frozen per-class nulls (E); OCT-6 recalibrate-and-continue (k-fit) added to the October decision menu + market-odds alpha variant; OCT-7 quota counting hardened (divergence/pre-audit/cluster); BR-1v3 requirements at next quarterly re-derive: vintage-pinning, walk-down decomposition, down-cycle backtest, per-name spread disclosure; PARSER-1 dumb-denominator + persistence-ratio monitors (queued to the prose-deadline sweep, due tomorrow); CAP-1 eviction log + compression-drift guard live now, tiered-architecture question to monthly audit.

**Position implication: 🟡 NO ACTION on the book — every amendment changes how evidence will be READ (above all: October's go/kill was structurally undecidable and mis-nulled as specified; it now has a decidable, recalibration-capable, capture-weighted design). The commissioning of all material instruments is COMPLETE two days after the discipline was conceived; ongoing cost drops to escorts + collision checks + monthly blind re-derives.**
