# The Newly-Viable Frontier — Mode A vs Mode B under a technology shock (framework, 2026-07-12)

**Origin:** user brain-dump 2026-07-12 EVE (verbatim-adjacent): prior cycles (mobile+data → Uber) show the breakthrough "in its infancy looks like a toy"; most actors chase "what can we optimize using LLMs" while the big companies come from "what LLM-native business models enable that previously wasn't possible — or was economically not viable"; X = the unknown unknown; asked for reframing into computable objectives. Direct continuity: Griffin dinner (same day, verified) = Mode A specimens; `wiki/agent-native-organization.md` founding recipe = the Mode B search this framework instruments; user's success definition ("discovering the unknown unknowns") = this file's purpose.

## 1. First principles
A technology shock moves the **feasibility frontier** in (capability, unit-economics) space: specific input costs collapse and new primitives appear. Two exploitation modes:
- **Mode A — optimization:** existing business model, cost line reduced. Value largely accrues to incumbents and competes away; frequently isn't even the new technology (Griffin dinner: ML/digitization mislabeled as AI).
- **Mode B — newly viable:** business models whose unit economics were NEGATIVE (or capability impossible) pre-shock, positive post-shock. The "toy" phase is Mode B before demand density arrives.

**The LLM shock's collapsed input:** unit cost of cognition/language work — reading, writing, classifying, judging, conversing (Arora −98%/token vs 3× bills; InferenceX ~60×/yr cost-per-quality, both on file in `sector/end-demand-durability-model.md` §2). **New primitives:** parallel tireless agents; per-user bespoke software; simulated conversation at scale; 24/7 polyglot judgment; near-zero marginal cost per additional "employee" (see `wiki/agent-native-organization.md` lift table).

## 2. The three computable objectives
1. **Viability-flip screen ("the Uber test"):** for any candidate business, recompute unit economics with PRE-2023 labor prices on every cognition-shaped COGS line. Contribution margin flips negative → Mode B (genuinely enabled). Survives → Mode A (optimization wearing AI clothes). Deterministic, per-candidate, runnable by agent.
2. **Historical base rate (pre-registered H1/H2/H3, my model 55/30/15):** H1 Mode-B dominance of new-company value creation at founding; H2 frontier-migration (start A or shallow-B, migrate deeper — Amazon pattern); H3 survivorship illusion (Mode B tail bigger, expectation worse — graveyard-adjusted). Verification agent fired 2026-07-12; results appended below when landed.
3. **Hunting map (the generative heuristic for X):** enumerate cost structures where human cognition >~50% of COGS AND latent demand exists at lower price points — "what did only the rich / the enterprise afford that cognition-pricing makes universal?" (Uber = private driver for everyone.) Second class: impossible-at-any-price primitives (per-user software, agent-to-agent negotiation markets, always-on personal representation). Phase-2 sweep pre-registered (todo P1): systematic enumeration + viability-flip screen on each candidate → ranked newly-viable candidate register.

## 3. Detection instruments (P#44 detection-over-prediction, not forecasting)
- **Toy-detector:** dismissed-category + strong retention/organic growth + unit economics that only work post-shock = Mode B in toy phase. (Historical validation: contemporaneous "toy" quotes register — agent task 5.)
- **Attribution filter (Griffin discount, same day):** exclude survey-class "AI gains" from Mode-B evidence; only revealed-preference (paying users, retention, capital committed) counts.
- **Crowding asymmetry:** count of Mode-A vs Mode-B startups/funding in the current cycle — if A-crowding >> B, the B side is where non-consensus lives (the user's core intuition).

## 4. Investable + founding linkage
- **Public-market expression is limited** (app-layer Mode B is mostly private; cf. agent-native-org tier answer: Tier-A doesn't exist). Indirect: Mode-B aggregate growth is the DURABLE demand leg of `sector/end-demand-durability-model.md` (coding agents = the first at-scale Mode B; TV#0 deceleration tripwire doubles as a Mode-B health gauge).
- **Founding linkage (the sharper use):** this framework is the SEARCH FUNCTION for the user's stated ambition (first LLM-native company; `agent-native-organization.md` founding recipe). The hunting map generates candidates; the viability-flip screen scores them; the toy-detector times them.

**Fluidity metadata:** framework codified 2026-07-12 (user-designed, N=1). Phase-2 hunting-map sweep = pre-registered next step (todo). Falsifier: if the historical agent returns H3-dominant (survivorship illusion), the hunting map must add a graveyard-rate denominator before any candidate ranking; if the phase-2 sweep produces only candidates that fail the viability-flip screen, the LLM shock's Mode-B window hasn't opened yet → revisit quarterly with worldview revision (2026-10-11).

## 5. Historical base-rate results (appended post-run)
_(agent in flight 2026-07-12 EVE)_
