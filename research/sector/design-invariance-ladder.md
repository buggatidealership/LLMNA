# DESIGN-INVARIANCE LADDER (born 2026-07-18, user thought experiment: "what part of the AI-chip supply chain is necessary regardless of how the chip is designed?") — seed frame for the overdue BOTTLENECK-FORECAST next-regime sweep

**Core move: separate FUNCTIONS (inescapable by any design) from IMPLEMENTATIONS (swappable by every design).** DRAM = implementation; state = function. Full ladder + margin-trap dissent in chat 2026-07-18 (this file = frame of record; sweep verifies the recall-tagged rows).

Tiers (invariance descending): (1) PHYSICS — energy/heat (power-integrity passives scale with TDP not architecture; MURATA held) + the wafer (all devices start as monocrystalline Si; SUMCO held) + patterning (litho/depo/etch + CONSUMABLES: resists/gases/slurries — coverage gap flagged); (2) FUNCTION-INVARIANT/IMPLEMENTATION-FLUID — state (any computer needs it; where it lives is swappable → memory names = implementation bets) + interconnect/packaging-as-category (heterogeneous integration gains under EVERY architecture road); (3) DESIGN-DEPENDENT — specific architectures (GPU/HBM/standards), disruptable.

Two structural findings: **(A) WAFER ANTIFRAGILITY TO THE MEMORY QUESTION** — SRAM-heavy DRAM-displacement (Cerebras/Groq-class, exists today) consumes MORE wafer/bit (~6T cell vs 1T1C, recall-based — verify), so every answer to "where does state live" buys wafers, exotic answers buy more. **(B) THE NECESSITY≠MARGIN TRAP** — most-invariant layers historically had worst economics (wafer commodity grind); durable profit = invariance × CONCENTRATION × qualification moat (ASML has all three, priced; wafers acquiring contract structure only now — TC-18 Micron-GlobalWafers 10yr instance). Rank plays by the product, not by necessity alone.

**Bias flag (honest):** book already holds SUMCO/MURATA — this ladder re-derives their logic; guard = the held theses predate the question. **Sweep tasks:** verify consumables-oligopoly structures (JSR/TOK/Shin-Etsu class, gases incl. helium seed), wafer-oligopoly shares, SRAM/DRAM area factor; screen the consumables layer for unheld candidates (the genuinely "less sexy" gap). Falsifier for the frame: a computing substrate shift off silicon at commercial scale inside 10 years would break tier-1 wafer invariance (tracked, P very low my model).
